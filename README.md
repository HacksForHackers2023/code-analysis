## Usage
* Install the required packages using `pip install -r requirements.txt` in the terminal.
* Run using the command `streamlit run main.py` in the terminal.

## Data Sources
* https://www.kaggle.com/datasets/aahuang/devpost-project-data
* https://www.kaggle.com/datasets/igalbronshtein/devpost-hackathons
